# Legacy Node/Next/Express Implementation

This folder preserves the previous monorepo implementation as a fallback archive.

Archived contents include:

- `apps/api` (Express + Prisma backend)
- `apps/web` (Next.js frontend)
- `packages/*` shared TypeScript packages
- `prisma/*` database schema and migrations
- legacy docs and workspace config

The repository root is now static-first for direct file-upload deployment.
